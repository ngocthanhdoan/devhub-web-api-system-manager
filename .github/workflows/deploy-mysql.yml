name: Deploy MySQL Server

on:
  push:
    branches:
      - devops-database-mysql-deploy

jobs:
  deploy-mysql:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Copy source code to server
      run: |
        scp -o StrictHostKeyChecking=no -r . ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/path/to/mysql/source/

    - name: Deploy to Ubuntu Server
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          cd /path/to/mysql/source/.docker/mysql
          docker-compose down
          docker rmi $(docker images -q mysql) || true
          docker-compose build
          docker-compose up -d
